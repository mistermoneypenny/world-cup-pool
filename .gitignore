node_modules/
data/*
!data/bonus-picks-baseline.json
!data/state-baseline.json
*.tmp
.DS_Store

# Local pick backups — kept for recovery but not committed
# (pre-deploy snapshots live in backups/ on your machine)
backups/*.json
